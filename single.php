<?php
    //CARPETA ARENISCA //
    if (in_category(11)) { // Si el post pertenece a la cat 6, redirigir a single.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca.php');
    } elseif (in_category(26)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca-content1.php');
    } elseif (in_category(27)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca-content2.php');
    } elseif (in_category(28)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca-content3.php');
    } elseif (in_category(29)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca-content4.php');
    } elseif (in_category(30)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/arenisca/single-arenisca-content5.php');
    //CARPETA ARENISCA //


    //CARPETA CALIZAS //
	} elseif (in_category(12)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas.php');
    } elseif (in_category(31)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/calizas/single-calizas-content5.php');
    //CARPETA CALIZAS //


    //CARPETA PREXURY //
    } elseif (in_category(13)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/prexury/single-prexury-content5.php');
    //CARPETA PREXURY //


    //CARPETA CUARCITA //
    } elseif (in_category(14)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/cuarcita/single-cuarcita-content5.php');
    //CARPETA CUARCITA //


    //CARPETA GRANITO //
    } elseif (in_category(15)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/granito/single-granito-content5.php');
    //CARPETA GRANITO //


    //CARPETA MARMOL //
    } elseif (in_category(16)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/marmol/single-marmoles-content5.php');
    //CARPETA MARMOL //


    //CARPETA NEOLITH //
    } elseif (in_category(17)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/neolith/single-neolith-content5.php');
    //CARPETA NEOLITH //


    //CARPETA ONIX //
    } elseif (in_category(18)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/onix/single-onix-content5.php');
    //CARPETA ONIX //


    //CARPETA PIZARRA //
    } elseif (in_category(19)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/pizarra/single-pizarra-content5.php');
    //CARPETA PIZARRA //


    //CARPETA PORFIDO //
    } elseif (in_category(20)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/porfido/single-porfido-content5.php');
    //CARPETA PORFIDO //


    //CARPETA SILESTONE //
    } elseif (in_category(21)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone-content2.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone-content3.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone-content4.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/silestone/single-silestone-content5.php');
    //CARPETA SILESTONE //


    //CARPETA TRAVERTINO //
    } elseif (in_category(22)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino-content1.php');
    } elseif (in_category(id)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/travertino/single-travertino-content1.php');
    //CARPETA TRAVERTINO //    

    //CARPETA TRABAJOS //
    } elseif (in_category(32)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/trabajos/single-trabajos-col1.php');
    } elseif (in_category(33)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/trabajos/single-trabajos-col2.php');
    } elseif (in_category(34)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/trabajos/single-trabajos-col3.php');
    } elseif (in_category(35)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/trabajos/single-trabajos-col4.php');
    } elseif (in_category(36)) { // Si el post pertenece a la cat 2, redirigir a single-2.php
    include(TEMPLATEPATH . '/trabajos/single-trabajos-col5.php');
    //CARPETA TRABAJOS //  
    
    
} ?>
